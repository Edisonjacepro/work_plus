{% extends 'base.html.twig' %}

{% block title %}Mes candidatures{% endblock %}

{% block body %}
    <h1>Mes candidatures</h1>

    <section>
        <h2>Mon impact candidat</h2>
        <p><strong>Points cumulés :</strong> {{ candidatePointsBalance }}</p>
        <p><strong>Niveau :</strong> {{ candidatePointsLevel }}</p>
    </section>

    {% if applications is empty %}
        <p>Vous n'avez pas encore postule.</p>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>Offre</th>
                    <th>Entreprise</th>
                    <th>Date</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for application in applications %}
                    <tr>
                        <td>{{ application.offer.title }}</td>
                        <td>{{ application.offer.company.name }}</td>
                        <td>{{ application.createdAt ? application.createdAt|date('d/m/Y H:i') : '-' }}</td>
                        <td><a href="{{ path('application_show', {id: application.id}) }}">Consulter</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}

    <section>
        <h2>Historique des points</h2>
        {% if candidatePointsHistory is empty %}
            <p>Aucun evenement de points pour le moment.</p>
        {% else %}
            <table>
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Type</th>
                        <th>Motif</th>
                        <th>Points</th>
                    </tr>
                </thead>
                <tbody>
                    {% for entry in candidatePointsHistory %}
                        <tr>
                            <td>{{ entry.createdAt ? entry.createdAt|date('d/m/Y H:i') : '-' }}</td>
                            <td>{{ entry.referenceType }}</td>
                            <td>{{ entry.reason }}</td>
                            <td>{{ entry.points > 0 ? '+' ~ entry.points : entry.points }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endif %}
    </section>
{% endblock %}
