{% extends 'base.html.twig' %}

{% block title %}Mes offres{% endblock %}

{% block body %}
    <h1>Mes offres</h1>
    <p><small>{{ totalItems }} resultat(s)</small></p>

    <p><a href="{{ path('offer_new') }}">Creer une offre</a></p>

    {% if offers is empty %}
        <p>Aucune offre.</p>
    {% else %}
        <ul>
            {% for offer in offers %}
                <li>
                    <a href="{{ path('offer_show', {id: offer.id}) }}">{{ offer.title }}</a>
                    - {{ offer.company.name }}
                    <span class="status-badge status-{{ offer.status|lower }}">
                        {{ pointsReasonLabelService.offerStatusLabel(offer.status) }}
                    </span>
                    <span class="status-badge status-{{ offer.moderationStatus|lower }}">
                        Moderation : {{ pointsReasonLabelService.offerModerationStatusLabel(offer.moderationStatus) }}
                    </span>
                </li>
            {% endfor %}
        </ul>

        {% if totalPages > 1 %}
            <nav aria-label="Pagination offres recruteur">
                {% if currentPage > 1 %}
                    <a href="{{ path('recruiter_offer_index', {page: currentPage - 1}) }}">Page precedente</a>
                {% endif %}
                <span>Page {{ currentPage }} / {{ totalPages }}</span>
                {% if currentPage < totalPages %}
                    <a href="{{ path('recruiter_offer_index', {page: currentPage + 1}) }}">Page suivante</a>
                {% endif %}
            </nav>
        {% endif %}
    {% endif %}
{% endblock %}
